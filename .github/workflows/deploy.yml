name: Deploy FastAPI to App Engine (Free Tier)

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      service_name:
        description: 'Name of the App Engine service'
        required: true
        default: 'fastapi-api'

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud CLI
        uses: google-github-actions/setup-gcloud@v2
        with:
          project_id: ${{ secrets.GCP_PROJECT }}
          version: 'latest'

      - name: Verify gcloud auth
        run: gcloud auth list

      - name: 📝 Generate app.yaml dynamically (FINAL ROBUST FIX)
        # 1. Define GitHub secrets as environment variables for this step using 'env:'
        env:
          SERVICE_NAME: ${{ github.event.inputs.service_name }}
          APP_SECRET_KEY: ${{ secrets.SECRET_KEY }}
          APP_ALGORITHM: ${{ secrets.ALGORITHM }}
          APP_ACCESS_TOKEN_EXPIRE_MINUTES: ${{ secrets.ACCESS_TOKEN_EXPIRE_MINUTES }}
          APP_DATABASE_URL: ${{ secrets.DATABASE_URL }}
          APP_GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          APP_ENVIRONMENT: ${{ secrets.ENVIRONMENT }}
          
        # 2. Use a simple shell script to generate app.yaml, relying on the 'env' variables
        run: |
          # Use an unquoted EOF (<<EOF) to allow shell variable expansion.
          # The shell substitutes all $VARIABLES with their secret values.
          cat > app.yaml <<EOF
          service: $SERVICE_NAME
          runtime: python310
          # \$PORT must be escaped so App Engine gets the literal string $PORT
          entrypoint: uvicorn app.main:app --host=0.0.0.0 --port=\$PORT

          automatic_scaling:
            max_instances: 1
            min_instances: 0

          env_variables:
            # Shell variables are substituted here and wrapped in quotes for valid YAML
            SECRET_KEY: "$APP_SECRET_KEY"
            ALGORITHM: "$APP_ALGORITHM"
            ACCESS_TOKEN_EXPIRE_MINUTES: "$APP_ACCESS_TOKEN_EXPIRE_MINUTES"
            DATABASE_URL: "$APP_DATABASE_URL"
            GEMINI_API_KEY: "$APP_GEMINI_API_KEY"
            ENVIRONMENT: "$APP_ENVIRONMENT"
          EOF

          echo "Generated app.yaml:"
          cat app.yaml

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Deploy to App Engine
        run: gcloud app deploy app.yaml --quiet --verbosity=info